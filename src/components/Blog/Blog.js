import React from 'react';

const Blog = () => {
    return (
        <div>
            <h3>Hoe React Works?</h3>
            <p>React is a JavaScript library which creats user interface with efficient way using declarative code.We can build single page application or mobile apps by using React.React implements virtual Dom three representation in javaScript.So </p>
            <h3>Difference between Props vs Stare?</h3>
            <p>by</p>
        </div>
    );
};

export default Blog;